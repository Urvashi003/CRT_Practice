#include<stdio.h>
int main(){
    // int n=5;
    // printf("value of n is %d\n" , n);
    // printf("add of n is %u" , &n);

    // int a , b , sum;
    // printf("enter 2 integers:");
    // scanf("%d%d", &a, &b);
    // sum = a+b;
    // printf("sum of %d and %d is %d", a , b , sum);
    // return 0;

    // int a, b;
    // float avg;
    // printf("enter 2 nos:");
    // scanf("%d %d", &a , &b);
    // avg= (float)(a+b)/2;
    // printf("avg of %d and %d is %.2f", a , b, avg);
    // return 0;

    // int a, b;
    // printf("enter 2 integers:");
    // scanf("%d %d", &a, &b);
    // printf("before swap %d %d", a, b);
    // // a=a+b;
    
    // // b=a-b;
    // // a=a-b;
    // a=b-a+(b=a);
    // printf("\n after swap %d %d", a, b);
    // return 0;
}